#include <iostream>
#include <vector>
#include <map>
#include <string>
#include <fstream>
#include <stdio.h>
#include <stdlib.h>
#include <assert.h>
#define DEBUG
using namespace std;

class Solution {
public:
    string passCount(int m, int n);
    void process(string infPath, string outfPath);
};

int main(int argc, char *argv[]) {
    Solution s;
    return 1;
}
